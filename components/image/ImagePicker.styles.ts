import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    width: '100%',
    marginBottom: 12,
  },
  previewWrapper: {
    marginTop: 0,
    marginBottom: 16, // Espaço entre a prévia e os botões
    alignItems: 'center',
    width: '100%',
  },
  buttonColumn: {
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  separator: {
    height: 12,
  },
});